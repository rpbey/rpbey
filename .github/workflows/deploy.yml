name: Deploy → rpb-dashboard

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'components/**'
      - 'lib/**'
      - 'public/**'
      - 'next.config.ts'
      - 'package.json'
      - 'bun.lock'
      - '!**.md'
  workflow_dispatch:
    inputs:
      skip_bot:
        description: "Skip le déploiement du bot"
        type: boolean
        default: false

concurrency:
  group: deploy-rpb-dashboard
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy rpb-dashboard → VPS
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          timeout: 900s
          command_timeout: 12m
          script: |
            set -euo pipefail
            SCRIPT="/home/ubuntu/rpb-dashboard/scripts/deploy.sh"
            ARGS=""
            [ "${{ github.event.inputs.skip_bot || 'false' }}" = "true" ] && ARGS="--skip-bot"

            echo "==> Deploy rpb-dashboard (commit: ${{ github.sha }})..."
            bash "$SCRIPT" $ARGS

      - name: Résumé
        if: always()
        run: |
          echo "### rpb-dashboard — ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Commit: \`${{ github.sha }}\` par ${{ github.actor }}" >> "$GITHUB_STEP_SUMMARY"
